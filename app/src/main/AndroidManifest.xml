<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mmuazekici.imdb250">

    <application
        android:allowBackup="true"
        android:icon="@drawable/movie"
        android:label="@string/app_name"
        android:roundIcon="@drawable/movie"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MovieListActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".SignupLogin.SignUpActivity" />
        <activity
            android:name=".SignupLogin.LogInActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MovieDetailsActivity"
                  android:label="Movie Details"/>
        <activity android:name=".FavoritesActivity"
                  android:label="Your Favorite Movies"/>
        <activity android:name=".ProfileActivity"
                  android:label="Profile"/>
        <activity android:name=".SearchActivity"
            android:label="Search"/>
        <activity android:name=".UsersFriends.UsersActivity"
                  android:label="Users"/>
        <activity android:name=".UsersFriends.FriendsActivity"
                  android:label="Your Friends"/>
        <activity android:name=".UsersFriends.FriendFavoritesActivity"></activity>
    </application>

</manifest>